<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@drawable/chevron_right"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:rotation="180"/>

        <TextView
            android:id="@+id/tvModuleTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Module Title"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginTop="12dp"/>

        <TextView
            android:id="@+id/tvSessionTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Session 1"
            android:textSize="16sp"
            android:textColor="#666"
            android:layout_marginBottom="20dp"/>

        <Button
            android:id="@+id/btnStartPre"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Start Pre-Assessment"
            android:layout_marginBottom="12dp"
            android:background="@drawable/btn_bg"
            android:textColor="#fff"/>

        <Button
            android:id="@+id/btnWatchVideos"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Watch Videos"
            android:layout_marginBottom="12dp"
            android:background="@drawable/btn_bg"
            android:textColor="#fff"/>

        <Button
            android:id="@+id/btnStartPost"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Start Post-Assessment"
            android:layout_marginBottom="12dp"
            android:background="@drawable/btn_bg"
            android:textColor="#fff"/>

        <Button
            android:id="@+id/btnRestart"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Restart Module (reset pre/post flags)"
            android:background="@android:color/darker_gray"
            android:textColor="#fff"/>

    </LinearLayout>
</ScrollView>
